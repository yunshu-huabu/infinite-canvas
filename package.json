{
    "name": "infinite-canvas-monorepo",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "bun scripts/dev.ts",
        "build": "bun run --cwd web build",
        "start": "bun server/src/index.ts",
        "test": "bun run --cwd server test && bun run --cwd web test",
        "typecheck": "bun run --cwd server typecheck && bun run --cwd web typecheck",
        "check": "bun run test && bun run typecheck && bun run build"
    }
}
